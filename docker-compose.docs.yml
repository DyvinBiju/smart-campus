services:
  docs:
    image: smart_campus_local_docs
    container_name: smart_campus_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./smart_campus:/app/smart_campus:z
    ports:
      - '9000:9000'
    command: /start-docs
